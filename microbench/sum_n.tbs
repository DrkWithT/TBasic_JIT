`test sumN vs. JIT`

FUN sumN(n):
    LET x : 0;

    WHILE n > 0:
        x := x + n;
        n := n - 1;
    END

    RET x;
END

LET x : 1000, dud : 0;

WHILE x > 0:
   dud := sumN(100);
   x := x - 1;
END

RET 1;